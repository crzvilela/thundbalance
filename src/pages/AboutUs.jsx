// DEPRECATED — no longer used.
//
// The standalone "/about-us" page was removed: its content (photo carousel
// + 360° embed) was migrated into the homepage's About section
// (see src/components/About.jsx and sections.about.carousel /
// sections.about.embed360Url in src/content/defaultContent.js).
//
// This file is no longer imported anywhere (removed from
// src/routes/AppRoutes.jsx) and is safe to delete manually — the assistant
// making this change did not have a file-delete tool available, only
// read/write, so it left this stub instead of a dangling broken import.
export default function AboutUsDeprecated() {
  return null
}
